{{- if eq .chezmoi.os "linux" -}}
{{- if eq .chezmoi.osRelease.id "debian" -}}
#!/bin/bash
chsh -s $(which zsh)
{{- end -}} {{/* if eq .chezmoi.osRelease.id "debian" */}}
{{- end -}} {{/* if eq .chezmoi.os "linux" */}}
